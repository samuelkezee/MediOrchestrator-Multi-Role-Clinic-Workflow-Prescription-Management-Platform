import { CanActivateFn } from '@angular/router';
import { GlobalConstants } from '../constants/GlobalConstants';
import { MenuConstant } from '../constants/Menu.Constants';
import { Userservices } from '../services/userservices';
import { inject } from '@angular/core';
import { Router } from '@angular/router';

export const roleBasedAccessGuard: CanActivateFn = (route, state) => {
  // debugger;
  // const usersrv=inject(Userservices);
  // const router=inject(Router)
  // const strArray: string[] = route.url.toString().split('/');
  // const moduleName = strArray[0];
  // const loggedUser = sessionStorage.getItem('hospitalUser') || localStorage.getItem('hospitalUser');


  // const menuItem = MenuConstant.menuItem.find(m => m.router == moduleName);
  // const isRoleExistInMenu=menuItem
  // // roles.includes(usersrv.loggedUserData);
  //   // roleName);

  // debugger;
  // if(isRoleExistInMenu){
  //   return true;
  // }else{  
  //   router.navigate(['/no-role-access']);
  //   return false;
  // } 
  return true;
};
